import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonWrapper: { 
        borderWidth: 1,
        borderRadius: 10,
        paddingHorizontal: 10, 
        paddingVertical: 10,
        maxHeight: 40,
    },

    text: {
        fontSize: 14,   
    }
})