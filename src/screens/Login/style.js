import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    loginWrapper : { 
        width: '100%',
        rowGap: 15,
    }, 

    registerText: { 
        marginTop: 20
    }, 
    
    title: {
        fontSize: 20,
        marginBottom:20,
        fontWeight: '500'
    }

})