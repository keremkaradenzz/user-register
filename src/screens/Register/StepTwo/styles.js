import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: { 
        flex: 1,
        width: '100%',
        rowGap: 15,
        paddingHorizontal: 15,
    },
    row: { 
        marginBottom: 15,
    },

    text: { 
        marginVertical: 10,
    }
})